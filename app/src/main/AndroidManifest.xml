<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RentalDemo"
        tools:targetApi="31">
        <activity
            android:name=".ui.feature.auth.login.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.RentalDemo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.feature.auth.register.RegisterActivity"
            android:exported="true" />

        <activity
            android:name=".ui.feature.auth.forgotpassword.VerifyEmailActivity"
            android:exported="true" />

        <activity
            android:name=".ui.feature.auth.forgotpassword.ResetPasswordActivity"
            android:exported="true" />

        <activity
            android:name=".ui.feature.main.landing.MainActivity"
            android:exported="true" />

        <activity
            android:name=".ui.feature.main.landing.detail.DetailActivity"
            android:exported="true" />
    </application>

</manifest>